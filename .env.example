# Production origin, without a path. Leave empty for local/demo use.
SITE_URL=
# Enable only after replacing sample content on the real production site.
ALLOW_INDEXING=false
# Enable only on a Netlify deployment with form detection enabled.
NEXT_PUBLIC_FORMS_ENABLED=false
